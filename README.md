# Cloud Development - Assignment 2

how to run:
```
npm install
```

```
npx prisma generate
```

```
npm run dev
```

to run docker, open the desktop app first and in your IDE terminal do:

```
docker compose up --build 
```

to generate the image, you can then access the website using the url after it has been build

to stop docker:
```
docker compose down
```